#include <bits/stdc++.h>

#define REP(i,n) for(int i=0;i<(int)(n);i++)
#define ALL(x) (x).begin(),(x).end()

using namespace std;

int main() {
  int a, b ,s, l;
	cin >> a >> b;
	s = a * b;
	l = (a + b) * 2;
	cout << s << " " << l << endl;
  return 0;
}
